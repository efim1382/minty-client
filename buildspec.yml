version: 0.2

phases:
    install:
        commands:
            - echo "Installing dependencies..."
            - npm install
    build:
        commands:
            - echo "Building..."
            - npm run build
            - cp package.json dist/

artifacts:
    base-directory: 'dist'
    files:
        - '**/*'
